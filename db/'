import requests
import os

class omdbAPIController:
    def __init__(self):
        self.key = os.getenv('OMDB_API_KEY')

    def createRequests(self, *kwargs):

    def findMovie(self, title: str = None, imdbID: str = None):
        pass


controller = omdbAPIController()
